`Index.html`-> La unica pagina real (SPA)
`css/style.css`-> Estilos basicos
`js/main.js` -> punto de arranque (boostrap)
`js/pages/home.js`-> pantalla Home(Inicio)
`js/pages/dashboard.js`-> pantalla Dashboard (Reporte de datos)


SPA
Necesita

- Unsolo html que nunca se recaga
- Un contenedor donde vamos cambiando vistas
- Un router que decide que vista a renderizar segun nombre/ruta
- Vistas separadas en modulos para que el poryecto crezca sin volverse caos


Usamos module en los scripts cuando necesitamos importar y exportar